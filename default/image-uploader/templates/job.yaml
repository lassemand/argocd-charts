apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world
spec:
  template:
    spec:
      containers:
      - name: hello-world-container
        image: lmapwns/argocd-image-uploader-test:0.0.x
      restartPolicy: Never